@echo off
rem Set the release to load code on demand (interactive) instead of preloading (embedded).
rem set RELEASE_MODE=interactive

rem Acknowledge that tailscale-rs is experimental software as required by the library.
set TS_RS_EXPERIMENT=this_is_unstable_software

rem Set the release to work across nodes.
rem RELEASE_DISTRIBUTION must be sname (local), name (distributed) or none.
rem set RELEASE_DISTRIBUTION=name
rem set RELEASE_NODE=<%= @release.name %>
